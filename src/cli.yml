name: minicon
version: "0.1.0"
author: smiyaguchi <smiyaguchi96@gmail.com>
about: minimal container runtime
subcommands:
    - run:
        about: run command create a container
        version: "0.1.0"
        args:
            - bundle:
                required: true
                help: Container bundle path.
            - command:
                required: true
                help: Container exec command.
            - arg:
                help: Container exec command args.
